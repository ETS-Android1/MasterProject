<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.masterproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MasterProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".DmvDocuments"></activity>
        <activity android:name=".CdphDocuments" />
        <activity
            android:name=".ui.login.LoginActivityDph"
            android:label="@string/title_activity_login_dph" />
        <activity android:name=".SchoolDocuments" />
        <activity android:name=".CurrentDocuments" />
        <activity android:name=".LogActivity" />
        <activity
            android:name=".ui.login.LoginActivityCsuf"
            android:label="@string/title_activity_login_csuf" />
        <activity android:name=".RequestedPage" />
        <activity android:name=".RequestCred" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>